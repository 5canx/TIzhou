
from django.db import models

# index/models.py
from django.db import models

# index/models.py
from django.db import models

class ESQuestionDummy(models.Model):
    class Meta:
        verbose_name = "ES 题库"
        verbose_name_plural = "ES 题库"
        managed = False  # 不创建表
